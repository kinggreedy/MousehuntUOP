BC
if (HIGH & VVS) VS
if (location == JoD && Cheese == VS || VVS) return to BC
if (VS qty == 0) LOCATION => JoD + CHEESE = GOUDA

SG
if (IN SG)
if (laststate != thisstate) => GET TRAP LIST => CHANGE TO BEST TRAP
if (CHEESE == CMC) => Gouda
if (charged == full && charm == charge charm) disarm charm //disarmTrinket
save state
if (IN ZT)
save state

FG
if (FG == OPEN & TIME LEFT < 16 min)
{
	save location & trap bait
	travel to FG
	arm trap bait
}
if (Location == AR)
{
	travel to saved location & trap bait
}

Furoma
IF (in FUROMA)
IF (BAIT QUANTITY == 0)
{
	GET FUROMA BAIT => IF (HAVE BAIT || CAN CRAFT) CHANGE LOCATION => ARM BAIT
	GET ITEM CURD > AMOUNT => POKE
	IF (FORCE MAKI ?) => GET MAKI BAIT => IF (HAVE BAIT) CHANGE LOCATION => ARM BAIT
	BRIE
}

FW
IF (in FW)
IF (0 <= STREAK <= LOWSTREAK) ARM PROPERTYPE CHARM & CHEESE 
IF (LOWSTREAK <= STREAK <= HIGHSTREAK) ARM SUPER CHARM & CHEESE
IF (WAVE == COMMANDER && GAGASTREAK > STREAK >= COMMANDERSTREAK) ARM COMMANDER
IF (WAVE == COMMANDER && COMMANDERGAGASTREAK <= STREAK) ARM GAGASETUP
IF (WAVE != COMMANDER && GAGASTREAK <= STREAK) ARM GAGASETUP

ZT Pawn
IF (in ZT)
IF (USING PAWN PINCHER && MOUSE > PAWN) CHANGE TO BEST TACTIC

TRAPCHECK CHANGE
IF (MODE == BEFORETRAPCHECK) change to trapcheck setup
ELSE (IF CUR SETUP != normal) change to normal setup

IB
IF (PHRASE 1 2 3 4) => BASE 1 2 3 4
IF (PHRASE 5 6) => DEEP FREEZE BASE
IF (SLUSHY SHORELINE && DEEP FREEZE BASE) => BEST BASE